version: 1
frontend:
    phases:
        preBuild:
            commands:
                - 'curl --proto ''=https'' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y'
                - 'source $HOME/.cargo/env'
                - 'curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh'
                - 'cd frontend && npm ci'
        build:
            commands:
                - 'cd $CODEBUILD_SRC_DIR/studyriscv-private/rust-core && wasm-pack build --target web --out-dir ../frontend/src/pkg'
                - 'export SKIP_PUBLIC_CHECK=1 && cd $CODEBUILD_SRC_DIR/studyriscv-private/frontend && npm install && npm run build'
    artifacts:
        baseDirectory: frontend/dist
        files:
            - '**/*'
    cache:
        paths:
            - 'frontend/node_modules/**/*'
            - '$HOME/.cargo/**/*'
