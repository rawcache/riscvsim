{
  "name": "riscvsim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "wasm:build": "cd ../rust-core && wasm-pack build --target web --out-dir ../frontend/src/pkg",
    "predev": "npm run wasm:build",
    "dev": "vite",
    "prebuild": "npm run wasm:build",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pretest": "npm run wasm:build",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "rollup": "^4.59.0",
    "typescript": "~5.9.3",
    "vite": "npm:rolldown-vite@7.2.5",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0",
    "vitest": "^4.1.0"
  },
  "overrides": {
    "vite": "npm:rolldown-vite@7.2.5"
  }
}
